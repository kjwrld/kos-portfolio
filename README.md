
  # Integrate React Flow

  This is a code bundle for Integrate React Flow. The original project is available at https://www.figma.com/design/KQc9PpebsTImjoHIUsntVh/Integrate-React-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  